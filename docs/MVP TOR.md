# 📄 Terms of Reference (TOR) — TableGamesBot (MVP)

Это ТЗ MVP проекта, то каким он должен быть в начале.

**Проект:** Телеграм-бот для организации и планирования игровых сессий в настольные игры (минимально жизнеспособный продукт).  
**Стек:** Node.js, Telegraf, Supabase, Pino.

---

## 1. Цель MVP
Запустить работающий инструмент, который позволит пользователям:
- регистрироваться в боте;
- создавать игровые встречи (директивно);
- записываться на встречи и отменять участие;
- получать уведомления о подтверждении участия;
- использовать лист ожидания для заполнения освободившихся мест.

Дополнительные функции (голосования, клубы, рейтинги, библиотека игр с API) будут добавлены после сбора обратной связи.

---

## 2. Функциональные требования (MVP)

### 2.1. Управление пользователями
*   **Регистрация**
    *   При первом запуске бот идентифицирует пользователя по Telegram ID.
    *   Данные сохраняются в таблицу `users`:
        *   `id` (уникальный)
        *   `tg_id`
        *   `username`
        *   `full_name`
        *   `city` (город, указывается пользователем)
*   **Профиль**
    *   Отображение основной информации.
    *   **Мои игры:** список предстоящих сессий, на которые записан пользователь.

### 2.2. Создание игровых сессий (только директивный режим)
*   Любой зарегистрированный пользователь может создать сессию, заполнив:
    *   **Название игры** (текстом, свободный ввод).
    *   **Дата и время** (ввод в свободном формате с последующим парсингом или выбор из календаря).
    *   **Место проведения** (текстом).
    *   **Максимальное количество игроков** (число).
*   После создания бот публикует сессию в общую ленту (см. п. 2.5).

### 2.3. Управление участием
*   **Запись на сессию**
    *   Пользователь нажимает кнопку «Записаться» под сообщением о сессии.
    *   Если мест нет, кнопка меняется на «В резерв».
*   **Отмена участия**
    *   Записавшийся пользователь может отменить своё участие (кнопка «Отменить запись»).
    *   При отмене место становится доступным.
*   **Лист ожидания (Waitlist)**
    *   Если все места заняты, пользователь может добавить себя в резерв.
    *   При освобождении места первый из резерва автоматически получает уведомление и переводится в основной список (подтверждение не требуется — место занимается автоматически, чтобы ускорить процесс).

### 2.4. Подтверждение участия (Check-in)
*   За 3 часа до начала сессии бот отправляет **всем записавшимся** уведомление с кнопками:
    *   ✅ «Буду точно»
    *   ❌ «Не смогу»
*   Если пользователь нажал «Не смогу» или **не ответил за 2 часа до начала**, он удаляется из списка участников, а его место передаётся первому из листа ожидания (если есть).
*   Организатор **не удаляется** автоматически — он может отменить сессию вручную.

### 2.5. Лента и уведомления
*   **Лента сессий**
    *   При входе в бот пользователь видит список ближайших сессий (всех, без фильтрации по городу — для MVP достаточно).
    *   Каждая сессия отображается карточкой: игра, дата, место, сколько мест занято / всего, организатор.
*   **Уведомления**
    *   При создании новой сессии бот отправляет уведомление **всем зарегистрированным пользователям** (для MVP это нормально, позже добавим фильтры).

### 2.6. Роль организатора
*   Создатель сессии помечается символом 👑.
*   Организатор может:
    *   отменить сессию (тогда все участники получают уведомление);
    *   вручную удалить любого участника из списка (например, если тот нарушает правила).

### 2.7. Интеграция с групповыми чатами (опционально для будущих версий)
*   Возможность подключения бота к групповому чату клуба.
*   Автоматическое создание отдельной ветки (темы) для каждой игровой сессии.
*   Уведомления о записи/отмене публикуются прямо в ветку сессии.
*   Все участники группы видят активные игровые сессии в виде отдельных тем.

---

## 3. База данных (Supabase)

### Таблица `users`
- `id` (uuid, primary key)
- `tg_id` (bigint, unique)
- `username` (text)
- `full_name` (text)
- `city` (text) — город, указанный при регистрации
- `created_at` (timestamp)

### Таблица `sessions`
- `id` (uuid, primary key)
- `game_name` (text)
- `datetime` (timestamp)
- `location` (text)
- `max_players` (int)
- `organizer_id` (uuid, references users.id)
- `status` (text) — 'active', 'cancelled', 'finished'
- `created_at` (timestamp)

### Таблица `participants`
- `id` (uuid, primary key)
- `session_id` (uuid, references sessions.id)
- `user_id` (uuid, references users.id)
- `status` (text) — 'registered', 'waiting', 'confirmed', 'no_show'
- `confirmed_at` (timestamp) — когда нажал «Буду точно»
- `created_at` (timestamp)

---

## 4. Что НЕ входит в MVP (будет позже)
- Демократическое создание сессий (голосования).
- Интеграция с BGG/Tesera.
- Рейтинги (Karma, Reputation).
- Клубы с ролями.
- Личные игры и разделение библиотеки.
- Иерархия локаций (город/район/точка) — только текстовое поле.
- Фильтрация ленты по городу/району.
- Возможность выставлять замену.

---

## 5. Критерии готовности MVP
- [ ] Пользователь может зарегистрироваться (данные сохраняются в БД).
- [ ] Пользователь может создать сессию, указав игру, дату/время, место, макс. игроков.
- [ ] Другие пользователи видят эту сессию в ленте и могут записаться.
- [ ] При заполнении мест появляется кнопка «В резерв».
- [ ] При отмене участия место автоматически передаётся первому из резерва.
- [ ] За 3 часа до начала всем записавшимся приходит запрос на подтверждение.
- [ ] Неподтвердившие удаляются, их места занимают резервисты.
- [ ] Организатор может отменить сессию или удалить участника.
- [ ] Все уведомления доставляются.

---

## 6. Модель распространения
Проект бесплатный, с возможностью добровольных пожертвований (донатов) через интегрированные сервисы (CloudTips, ЮMoney). Реклама и подписки не планируются.