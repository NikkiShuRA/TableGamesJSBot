# 📄 Terms of Reference (TOR) — TableGamesBot (MVP)

Это ТЗ для MVP проекта

**Проект:** Телеграм-бот для организации и планирования игровых сессий в настольные игры.  
**Стек:** Node.js, Telegraf, Supabase, Pino.

---

## 1. Цель MVP
Запустить работающий инструмент, который позволит пользователям:
- регистрироваться в боте;
- создавать игровые встречи двумя способами: директивно (организатор всё задаёт сам) и демократически (голосование за игру, время и место);
- записываться на встречи и отменять участие;
- получать уведомления о подтверждении участия;
- использовать лист ожидания для заполнения освободившихся мест.

---

## 2. Функциональные требования (MVP)

### 2.1. Управление пользователями
*   **Регистрация**
    *   При первом запуске бот идентифицирует пользователя по Telegram ID.
    *   Данные сохраняются в таблицу `users`:
        *   `id` (уникальный)
        *   `tg_id`
        *   `username`
        *   `full_name`
        *   `city` (город, указывается пользователем)
*   **Профиль**
    *   Отображение основной информации.
    *   **Мои игры:** список предстоящих сессий, на которые записан пользователь.

### 2.2. Создание игровых сессий

#### 2.2.1. Директивный режим
*   Любой зарегистрированный пользователь может создать сессию, заполнив:
    *   **Название игры** (текстом, свободный ввод).
    *   **Дата и время** (ввод с последующим парсингом или выбор из календаря).
    *   **Место проведения** (текстом).
    *   **Максимальное количество игроков** (число).

#### 2.2.2. Демократический режим (голосование)
*   Инициатор (не обязательно будущий организатор) запускает создание сессии через голосование.
*   Голосование проходит **последовательно**:
    1. **Выбор игры:** участники предлагают варианты (текстом). После сбора предложений (по команде инициатора или по таймеру) формируется список для голосования. Каждый участник может отдать один голос за понравившийся вариант. Победитель определяется простым большинством.
    2. **Выбор даты и времени:** аналогично — сбор предложений, затем голосование.
    3. **Выбор места:** сбор предложений, затем голосование.
*   После завершения всех голосований автоматически создаётся сессия с выбранными параметрами. Инициатор становится организатором.
*   **Упрощения для MVP:**
    *   Нет ограничений по времени голосования (инициатор вручную завершает этап кнопкой «Завершить приём предложений» / «Завершить голосование»).
    *   Нет сложных правил (ничья разрешается случайным выбором или повторным голосованием).
    *   Все участники, которые когда-либо взаимодействовали с ботом, могут участвовать в голосовании.

### 2.3. Управление участием
*   **Запись на сессию**
    *   Пользователь нажимает кнопку «Записаться» под сообщением о сессии.
    *   Если мест нет, кнопка меняется на «В резерв».
*   **Отмена участия**
    *   Записавшийся пользователь может отменить своё участие (кнопка «Отменить запись»).
    *   При отмене место становится доступным.
*   **Лист ожидания (Waitlist)**
    *   Если все места заняты, пользователь может добавить себя в резерв.
    *   При освобождении места первый из резерва автоматически получает уведомление и переводится в основной список.

### 2.4. Подтверждение участия (Check-in)
*   За 3 часа до начала сессии бот отправляет **всем записавшимся** уведомление с кнопками:
    *   ✅ «Буду точно»
    *   ❌ «Не смогу»
*   Если пользователь нажал «Не смогу» или **не ответил за 2 часа до начала**, он удаляется из списка участников, а его место передаётся первому из листа ожидания (если есть).
*   Организатор **не удаляется** автоматически.

### 2.5. Лента и уведомления
*   **Лента сессий**
    *   При входе в бот пользователь видит список ближайших сессий (всех, без фильтрации).
    *   Каждая сессия отображается карточкой: игра, дата, место, сколько мест занято / всего, организатор.
*   **Уведомления**
    *   При создании новой сессии бот отправляет уведомление **всем зарегистрированным пользователям**.

### 2.6. Роль организатора
*   Создатель сессии (в директивном режиме) или инициатор (в демократическом) помечается символом 👑.
*   Организатор может:
    *   отменить сессию (тогда все участники получают уведомление);
    *   вручную удалить любого участника из списка.

### 2.7. Интеграция с групповыми чатами (по возможности)
* Вариант 1 - Ветки сообщества:
    *   Возможность подключения бота к групповому чату клуба.
    *   Автоматическое создание отдельной ветки (темы) для каждой игровой сессии.
    *   Уведомления о записи/отмене публикуются прямо в ветку сессии.
    *   Все участники группы видят активные игровые сессии в виде отдельных тем.
* Вариант 2 - Группы:
    *   Администратор проекта заранее создаёт несколько пустых групп (например, 20-30 штук)
    *   Бот хранит их ID в базе данных как "свободные"
    *   При создании сессии бот:
        *   Берёт свободную группу из пула
        *   Переименовывает её под конкретную игру
        *   Добавляет всех участников
        *   Настраивает права
    *   После завершения игры группу можно очистить и вернуть в пул

---

## 3. База данных (Supabase)

### Таблица `users`
- `id` (uuid, primary key)
- `tg_id` (bigint, unique)
- `username` (text)
- `full_name` (text)
- `city` (text)
- `created_at` (timestamp)

### Таблица `sessions`
- `id` (uuid, primary key)
- `game_name` (text)
- `datetime` (timestamp)
- `location` (text)
- `max_players` (int)
- `organizer_id` (uuid, references users.id)
- `status` (text) — 'active', 'cancelled', 'finished'
- `created_at` (timestamp)
- `voting_id` (uuid, references votings.id, nullable) — связь с голосованием, если сессия создана демократически

### Таблица `votings`
- `id` (uuid, primary key)
- `initiator_id` (uuid, references users.id)
- `current_stage` (text) — 'game', 'datetime', 'location', 'finished'
- `created_at` (timestamp)

### Таблица `voting_options`
- `id` (uuid, primary key)
- `voting_id` (uuid, references votings.id)
- `stage` (text) — к какому этапу относится вариант ('game', 'datetime', 'location')
- `value` (text) — текст варианта (название игры, слот времени, место)
- `proposed_by` (uuid, references users.id) — кто предложил

### Таблица `votes`
- `id` (uuid, primary key)
- `voting_id` (uuid, references votings.id)
- `stage` (text)
- `option_id` (uuid, references voting_options.id)
- `user_id` (uuid, references users.id)
- `created_at` (timestamp)

### Таблица `participants`
- `id` (uuid, primary key)
- `session_id` (uuid, references sessions.id)
- `user_id` (uuid, references users.id)
- `status` (text) — 'registered', 'waiting', 'confirmed', 'no_show'
- `confirmed_at` (timestamp)
- `created_at` (timestamp)

---

## 4. Что НЕ входит в MVP (будет позже)
- Интеграция с BGG/Tesera.
- Рейтинги (Karma, Reputation).
- Клубы с ролями.
- Личные игры и разделение библиотеки.
- Иерархия локаций (только текстовое поле).
- Фильтрация ленты по городу/району.
- Возможность выставлять замену (кроме автоматического листа ожидания).
- Сложные правила голосования (ничьи, таймеры, кворум).
- Групповые чаты и ветки.

---

## 5. Критерии готовности MVP
- [ ] Регистрация пользователя.
- [ ] Директивное создание сессии.
- [ ] Демократическое создание сессии (последовательные этапы: предложения, голосование, определение победителя).
- [ ] Просмотр ленты сессий.
- [ ] Запись / отмена записи.
- [ ] Лист ожидания с автоматическим зачислением при освобождении места.
- [ ] Уведомление о подтверждении за 3 часа до начала.
- [ ] Автоудаление неподтвердивших и передача мест резерву.
- [ ] Возможность организатора отменить сессию или удалить участника.

---

## 6. Модель распространения
Проект бесплатный, с возможностью добровольных пожертвований через CloudTips / ЮMoney. Реклама и подписки не планируются.